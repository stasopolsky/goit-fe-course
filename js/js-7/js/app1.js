
'use strict';
// Напиши скрипт, который выполнит следующие операции.
// + Посчитает и выведет в консоль количество категорий в ul#categories, то есть элементов li.item. Получится 'В списке 3 категории.'.
// + Для каждого элемента li.item в списке ul.list, найдет и выведет в консоль текст заголовка элемента (теге h2) и количество элементов в категории (всех вложенных в него элементов li).
// Например для первой категории получится:
// Категория: Животные
// Количество элементов: 4
// function log(callback) {
//   console.log(callback);
// }
// function dir(callback) {
//   console.dir(callback);
// }
// const categories = document.querySelector('#categories');
// // const categoriesLength = categories.childElementCount
// const categoriesLength = categories.children.length;
// log(`В списке ${categoriesLength} категории.`);
// const categoriesName = categories.querySelectorAll('.item').forEach(e =>
//   log(`  Категория: ${e.children[0].innerText}
//   Количество элементов: ${e.children[1].children.length}`),
// );
